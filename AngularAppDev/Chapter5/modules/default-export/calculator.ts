// 默认导出加法函数add()
export default function add(x, y) {
    return x + y;
}

// 导出减法函数subtract()
export function subtract(x, y) {
    return x - y;
}

// 导出减法函数subtract()
export function subtract1(x, y) {
    return x - y;
}
